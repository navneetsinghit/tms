<%= form_for @competency do |f| %>
<p>
	<%= f.label :product_name %><br />
	<%= @product_name%>
</p>

<p>
	<%= f.label :content %><br />
	<%= f.text_area :content%>
</p>
	
	<p><%= f.submit :Submit%></p>
<% end %>
<%= link_to 'Back', articles_path %>